#include <bits/stdc++.h>
using namespace std;

#define

unsigned long long hash_func(string &goal, unsigned hash_code_len) {
    unsigned long long hash_code = 0;
    for (int i = 0; i < goal.length(); ++i) {
        hash_code +=
    }
}

int main() {

}